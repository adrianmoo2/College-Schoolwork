To run the code, just go to the relevant directory and run it.

The code isn't complete (part 7 isn't). For test4.csv, the numbers got mushed together when I put them all in one line,
so I broke it up into multiple lines. There are multiple line breaks (enters) to distinguish between the two recurrence equations.